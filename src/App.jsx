import{useState}from"react";import{newProduction}from"./types/production";import{productionStore}from"./persistence/productionStore";import{useStudioController}from"./state/useStudioController";import Startup from"./components/Startup";import Shell from"./components/Shell";
export default function App(){const[production,setProduction]=useState(()=>productionStore.getActive());const controller=useStudioController();if(!production)return <Startup productions={productionStore.list()} onCreate={data=>setProduction(productionStore.save(newProduction(data)))} onOpen={id=>{productionStore.setActive(id);setProduction(productionStore.get(id));}}/>;const save=next=>setProduction(productionStore.save(next));return <Shell production={production} onProductionChange={save} onClose={()=>setProduction(null)} controller={controller}/>;}
