import { migrateProduction } from "../types/production";
const INDEX="vero-studio-productions-v1",ACTIVE="vero-studio-active-production-v1",SETTINGS="vero-studio-settings-v1";
const parse=(raw,fallback)=>{try{return raw?JSON.parse(raw):fallback;}catch{return fallback;}};
export const productionStore={list(){return parse(localStorage.getItem(INDEX),[]).map(migrateProduction).sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt));},save(production){const next={...migrateProduction(production),updatedAt:new Date().toISOString()};const list=this.list();const index=list.findIndex(item=>item.productionId===next.productionId);if(index>=0)list[index]=next;else list.unshift(next);localStorage.setItem(INDEX,JSON.stringify(list));localStorage.setItem(ACTIVE,next.productionId);return next;},get(id){return this.list().find(item=>item.productionId===id)||null;},getActive(){return this.get(localStorage.getItem(ACTIVE));},setActive(id){localStorage.setItem(ACTIVE,id);},settings(){return parse(localStorage.getItem(SETTINGS),{});},saveSettings(value){localStorage.setItem(SETTINGS,JSON.stringify(value));return value;}};
