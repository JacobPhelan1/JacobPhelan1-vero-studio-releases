export default function InputsPage({controller}){const state=controller.vmix.state;return <section className="panel-page"><header><p>VMIX SOURCES</p><h2>Input inventory</h2><span>{state?.inputs.length||0} detected</span></header><div className="data-table"><div><b>#</b><b>NAME</b><b>TYPE</b><b>STATE</b><b>AUDIO</b></div>{state?.inputs.map(i=><div key={i.key||i.number}><b>{i.number}</b><strong>{i.name}</strong><span>{i.type}</span><span>{i.number===state.active?"Program":i.number===state.preview?"Preview":i.state||"Ready"}</span><span>{i.audio?(i.audio.muted?"Muted":`${Math.round(i.audio.volume)}%`):"Not exposed"}</span></div>)}</div></section>}
