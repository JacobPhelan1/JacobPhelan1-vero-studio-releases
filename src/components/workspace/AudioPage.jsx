export default function AudioPage({controller}){const inputs=controller.vmix.state?.inputs.filter(i=>i.audio)||[];return <section className="panel-page"><header><p>BASIC MIXER</p><h2>Production audio</h2><span>{inputs.length?"Real vMix state":"No audio data"}</span></header><div className="audio-grid">{inputs.map(i=><article key={i.key||i.number}><header><span>INPUT {i.number}</span><strong>{i.name}</strong></header><div className="fader"><input type="range" min="0" max="100" value={i.audio.volume} onChange={e=>controller.setVolume(i.number,e.target.value)}/><b>{Math.round(i.audio.volume)}%</b></div><button className={i.audio.muted?"muted":""} onClick={()=>controller.toggleMute(i.number,i.audio.muted)}>{i.audio.muted?"UNMUTE":"MUTE"}</button><small>Live meters are unavailable in the current Bridge contract.</small></article>)}</div>{!inputs.length&&<div className="placeholder"><strong>Audio controls are waiting for vMix.</strong><p>No meter activity is simulated. Supported channels appear when real input audio state is returned.</p></div>}</section>}
