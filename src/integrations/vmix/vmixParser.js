const number=(value,fallback=0)=>Number.isFinite(Number(value))?Number(value):fallback;
export function parseVmixState(xml){const doc=new DOMParser().parseFromString(xml,"application/xml");if(doc.querySelector("parsererror")||doc.documentElement.localName.toLowerCase()!=="vmix")throw new Error("vMix returned invalid production XML.");const inputs=[...doc.querySelectorAll("inputs > input")].map((node)=>({key:node.getAttribute("key")||"",number:number(node.getAttribute("number")),name:node.getAttribute("title")||`Input ${node.getAttribute("number")}`,type:node.getAttribute("type")||"Unknown",state:node.getAttribute("state")||"",thumbnail:node.getAttribute("thumbnail")||"",audio:node.getAttribute("muted")!==null?{muted:node.getAttribute("muted")==="True",volume:number(node.getAttribute("volume"),100)}:null}));const active=number(doc.querySelector("active")?.textContent),preview=number(doc.querySelector("preview")?.textContent);return{version:doc.querySelector("version")?.textContent||"",edition:doc.querySelector("edition")?.textContent||"",active,preview,inputs,recording:doc.querySelector("recording")?.textContent==="True",streaming:doc.querySelector("streaming")?.textContent==="True",master:{volume:number(doc.querySelector("audio > master")?.getAttribute("volume"),100),muted:doc.querySelector("audio > master")?.getAttribute("muted")==="True"}};}
