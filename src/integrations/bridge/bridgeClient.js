import { APP_CONFIG } from "../../config/appConfig";
async function health(baseUrl,path,label){const response=await fetch(`${baseUrl}${path}`,{cache:"no-store",signal:AbortSignal.timeout(2500)});if(!response.ok)throw new Error(`${label} returned HTTP ${response.status}.`);const type=response.headers.get("content-type")||"";if(!type.includes("application/json"))throw new Error(`${label} returned an invalid response.`);const result=await response.json();if(!result.ok)throw new Error(result.error||`${label} is unavailable.`);return result;}
export class BridgeClient{constructor(baseUrl){this.baseUrl=(baseUrl||window.veroDesktop?.studioEngineUrl||APP_CONFIG.services.studioEngineUrl).replace(/\/$/,"");}health(){return health(this.baseUrl,"/__vero/studio/health","Studio Engine");}async publishContext(context){const response=await fetch(`${this.baseUrl}/__vero/studio/context`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(context),signal:AbortSignal.timeout(2500)});if(!response.ok)throw new Error(`Context endpoint returned HTTP ${response.status}.`);return response.json();}}
export class LocalBridgeClient{constructor(baseUrl){this.baseUrl=(baseUrl||APP_CONFIG.services.localBridgeUrl).replace(/\/$/,"");}health(){return health(this.baseUrl,"/__vero/bridge/health","VERO Local Bridge");}}
