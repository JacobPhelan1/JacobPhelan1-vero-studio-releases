import { APP_CONFIG } from "../../config/appConfig";
export class BridgeClient{constructor(baseUrl){this.baseUrl=(baseUrl||window.veroDesktop?.bridgeUrl||APP_CONFIG.defaultBridgeUrl).replace(/\/$/,"");}async health(){const response=await fetch(`${this.baseUrl}/__vero/bridge/health`,{cache:"no-store",signal:AbortSignal.timeout(2500)});if(!response.ok)throw new Error(`Bridge returned HTTP ${response.status}.`);return response.json();}async vmix(config,params={}){const query=new URLSearchParams({host:config.host,port:String(config.port),...params});const response=await fetch(`${this.baseUrl}/__vero/vmix?${query}`,{cache:"no-store",signal:AbortSignal.timeout(APP_CONFIG.vmix.timeoutMs)});const text=await response.text();if(!response.ok)throw new Error((()=>{try{return JSON.parse(text).error;}catch{return text;}})()||`vMix returned HTTP ${response.status}.`);return text;}}
